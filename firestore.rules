rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Single document for app data; allow read/write for now.
    // Restrict with auth when you add Firebase Auth (e.g. request.auth != null).
    match /appData/{docId} {
      allow read, write: if true;
    }
  }
}
